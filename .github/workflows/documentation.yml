version: 2

sphinx:
  configuration: docs/conf.py

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"

python:
  install:
    - requirements: docs/requirements.txt
    # optional: also install your package itself
    - method: pip
      path: .
